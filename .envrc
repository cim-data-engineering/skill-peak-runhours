# Unset CodeArtifact env vars to prevent uv resolving against wrong registry
unset UV_INDEX_URL UV_EXTRA_INDEX_URL CODEARTIFACT_AUTH_TOKEN PIP_INDEX_URL 2>/dev/null || true

# Activate virtualenv (run "uv sync" first to create it)
if [ -d .venv ]; then
  source .venv/bin/activate
fi
