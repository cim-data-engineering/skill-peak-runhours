[project]
name = "skill-peak-runhours"
version = "0.1.0"
description = "PEAK run-hours skill plus the MCP/timeseries eval harness (investigation branch)"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.4",
    "matplotlib>=3.11.0",
    "numpy>=2.5.0",
    "pyarrow>=24.0.0",
]

[tool.ruff]
target-version = "py313"
